Q204 (route across two Regions, exclude unhealthy) — went with B (Route 53 multivalue answer + health checks). Failover routing would also be defensible; the options didn't include it cleanly.
Q332 (prevent accidental EBS snapshot deletion) — D (EBS Snapshot Lock, released late 2023). If your study source predates that, C (Recycle Bin) was the older canonical answer.
Q462 (TCP+UDP, multi-region, low latency) — went with [A, C] (internal NLBs + Global Accelerator). The options described NLBs as "internal" rather than "internet-facing," which is non-standard wording but does work with Global Accelerator endpoints.
Q677 (Sunday 12-hour workload, can't tolerate interruption) — A uses Scheduled Reserved Instances. AWS deprecated those a while back, but the option is still in the question pool, so it remains the best fit among the four. In practice today you'd use an Instance Scheduler or EventBridge to start/stop On-Demand or a Savings Plan that covers both windows.